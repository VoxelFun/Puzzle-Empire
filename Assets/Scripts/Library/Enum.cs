
public enum ContenderColor {
    Red, Blue, Green, Yellow
}

public enum ContenderRace {
    Humans, Undeads
}

public enum MoveType {
    Field, Map, WinBattle
}

public enum PlayerType {
    Neutral = -1, Local, Computer
}

public enum Gem {
    Random = -2, None, Red, Blue, Green, Yellow, Purple, White, Skull, SkullPlus, Multiplier
}

public enum EffectName {
    AdditionalMove,
    ArmorBuff,
    ArmorReduction,
    AutoAttackBuff,
    Blind,
    BlockOfAutoAttack,
    Charging,
    DamagePerTurn,
	Delay,
    Freeze,
    Shield,
    Silent,
    Stun,
    Bleeding,
}